{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run C++ (GCC)",
      "type": "shell",
      "command": "g++",
      "args": [
        "-std=gnu++17",
        "${file}",
        "-o",
        "${fileBasenameNoExtension}"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}